document.addEventListener('DOMContentLoaded', function() { const form = document.getElementById('downloadForm'); const urlInput = document.getElementById('youtubeUrl'); const adModal = document.getElementById('adModal'); const closeAdBtn = document.getElementById('closeAdBtn'); const downloadBtn = document.getElementById('downloadBtn'); const qualitySelect = document.getElementById('quality'); const formatSelect = document.getElementById('format'); const options = document.getElementById('options'); const successMessage = document.getElementById('successMessage'); form.addEventListener('submit', function(e) { e.preventDefault(); const url = urlInput.value.trim(); if (!url) { alert('Por favor, cole um link do YouTube!'); return; } showAdModal(); }); closeAdBtn.addEventListener('click', function() { adModal.classList.remove('show'); options.classList.add('show'); }); downloadBtn.addEventListener('click', function() { const url = urlInput.value.trim(); const quality = qualitySelect.value; const format = formatSelect.value; if (!url) { alert('Por favor, cole um link primeiro!'); return; } console.log('Iniciando download...'); console.log('URL:', url); console.log('Qualidade:', quality); console.log('Formato:', format); successMessage.classList.add('show'); setTimeout(() => { successMessage.textContent = 'Seu vídeo está sendo processado! Baixe começará em breve...'; }, 1000); setTimeout(() => { simulateDownload(url, quality, format); }, 2000); }); qualitySelect.addEventListener('change', function() { console.log('Qualidade selecionada:', this.value); }); formatSelect.addEventListener('change', function() { console.log('Formato selecionado:', this.value); }); function showAdModal() { adModal.classList.add('show'); options.classList.remove('show'); successMessage.classList.remove('show'); } function simulateDownload(url, quality, format) { const message = `Vídeo baixado com sucesso!\nQualidade: ${quality}\nFormato: ${format}\n\nEm um projeto real, o arquivo seria baixado aqui.`; successMessage.textContent = message; alert(message); } });